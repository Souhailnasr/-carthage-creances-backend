spring.application.name=Carthage-Creance_backend
# Server Configuration
server.port=8089
server.servlet.context-path=/carthage-creance
# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/carthage_creances?useUnicode=true&createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Prefer Hibernate's modern dialect resolution or specify database platform explicitly for Boot 3 / Hibernate 6
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Logging
logging.level.com.zaxxer.hikari=WARN
logging.level.org.springframework=WARN
logging.level.root=INFO
logging.level.projet.carthagecreance_backend.Config=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %-5level - %logger{60} - %msg%n

# App feature flags
app.validation.enabled=false

# JWT Configuration
# JWT Token Configuration
jdj.secure.token.validity=3600

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
spring.servlet.multipart.file-size-threshold=2MB

# Database Migration Configuration (Flyway - optional)
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration
# spring.flyway.baseline-on-migrate=true

# Database Migration Configuration (Liquibase - optional)
# spring.liquibase.enabled=true
# spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
